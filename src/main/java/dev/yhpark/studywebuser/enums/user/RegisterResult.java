package dev.yhpark.studywebuser.enums.user;

public enum RegisterResult {
    FAILURE,
    FAILURE_EMAIL_DUPLICATE,
    FAILURE_EMAIL_NOT_VERIFIED,
    FAILURE_NICKNAME_DUPLICATE,
    FAILURE_CONTACT_DUPLICATE,
    SUCCESS
}